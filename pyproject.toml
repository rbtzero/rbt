[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "splitstep_toe"
version = "0.0.1"
authors = [ { name = "RBT" } ]
dependencies = [
    "numpy",
] 